# Revisión adversarial pre-merge de Inspectra 0.3.0-beta.1

Estado: **GO para que el mantenedor decida, mediante autorización separada, si
marca el PR como listo. Este informe no constituye esa autorización**.

## Identidad congelada

- PR: [#1](https://github.com/dreykdrk7/inspectra/pull/1), abierto y en borrador.
- Base remota y merge-base: `e027c4509ba1c726554a639c5f06a076d35132c8`.
- HEAD inicial local, remoto y del PR: `3dbf2676630a86bca8241f11bf642db091a7c7a1`.
- Alcance inicial: 19 commits, 372 archivos, +149.046/-5.005 líneas. No hay
  binarios, submódulos, renombres, borrados ni cambios de modo; el único archivo
  mayor de 1 MiB es código de pruebas.
- Estado inicial: árbol limpio, rama alineada con upstream, ninguna operación
  Git incompleta, PR mergeable y cuatro jobs CI verdes sobre el HEAD inicial.
- Último SHA funcional revisado y publicado:
  `5439dddcf9b451c38b75c7c500629dbca6634feb` (21 commits, 374 archivos,
  +149.476/-5.008 frente a `main`). PR abierto, draft, mergeable y `CLEAN`.

## Método

Se revisó `main...HEAD` sin aceptar afirmaciones previas como evidencia. Las
cinco pasadas usaron datos sintéticos, fixtures y servicios locales; no se
analizó un proyecto real ni hubo tráfico Active externo. Las correcciones se
limitaron a tres defectos P1 reproducibles; los P2 no triviales se registraron.

## Matriz de hallazgos

| ID | Prioridad | Evidencia | Impacto | Decisión |
| --- | --- | --- | --- | --- |
| PMR-001 | P1 | El probe Active validaba DNS y después `HTTPConnection`/`HTTPSConnection` resolvía otra vez el hostname. | Rebinding DNS podía eludir la política SSRF en una capacidad opt-in. | **Corregido:** el socket conecta a la IP global validada y conserva hostname para `Host`, SNI y certificado. Regresión determinista sin Internet. |
| PMR-002 | P1 | Los CSV de remediación emitían texto no confiable con prefijos `=`, `+`, `-`, `@`, tab, CR o LF. | Fórmula ejecutable al abrir un informe en una hoja de cálculo. | **Corregido:** neutralización visible en ambos CSV; JSON conserva el dato original. |
| PMR-003 | P2 | El webhook configurado por operador valida DNS, pero `httpx` vuelve a resolver al conectar. | Riesgo residual de rebinding si se habilita; payload limitado a IDs opacos. | Diferido como `PROD-256`; mantener deshabilitado y usar DNS confiable. |
| PMR-004 | P2 | A 320 px se midieron controles de 16–40 px, aunque no hubo overflow a 320/390/768/1440 px. | Menor usabilidad táctil; el flujo sigue siendo completable. | Diferido como `PROD-257`; requiere revisión transversal. |
| PMR-005 | P2 | GitHub devuelve `404 Branch not protected` para `main` y no hay rulesets. | Los checks existen, pero no son exigibles antes de integrar. | `PROD-259` bloqueada hasta autorización expresa para mutar gobierno remoto. |
| PMR-006 | P2 | No existe `SECURITY.md` ni canal privado verificable documentado. | Reportes sensibles podrían terminar en issues públicos. | Diferido como `PROD-258`, pendiente de un canal real. |
| PMR-007 | P1 | El gate Active falló 4/12 observaciones (p95 pared 1,002–1,042 s) por mezclar CPU con pausas del host. | CI intermitente sin discriminar regresión algorítmica. | **Corregido:** CPU p95 sigue `<1 s`; pared tiene guarda `<2 s`; 40.000 lecturas y `<128 MiB` permanecen. Diez repeticiones y suite completa verdes. |

## Pasada 1 — seguridad y privacidad

- Se inspeccionaron sesión/autenticación, CSRF, bearer tokens, roles, stores
  owner-scoped, archivos, redacción, logs, HTML/React y egress. No se confirmó
  IDOR, escalada o fuga adicional.
- ZIP/TAR, nombres, enlaces, bombas, conteos y tamaños fallan cerrados. La
  inteligencia pública usa destinos fijos, HTTPS, sin redirecciones/proxy
  ambiental y límites de tiempo, tamaño, concurrencia, reintento y caché.
- Se reprodujeron y corrigieron PMR-001/002. El webhook queda separado como
  PMR-003/`PROD-256`.
- Gitleaks sobre el rango inicial exacto (19 commits, ~7,42 MiB) no halló
  secretos. El canario detectó exactamente la fuga sintética esperada. Las 54
  señales de un `git archive` sin contexto Git se reconciliaron como fixtures,
  pruebas o documentación cubiertos por el contrato canónico; no se imprimieron
  ni conservaron valores.

## Pasada 2 — integridad funcional, datos y actualización

- Un checkout limpio de la base `e027c450…` creó un manifiesto y un trabajo
  completado sintéticos. La candidata leyó 1/1 registros y preservó payload,
  metadatos y estado.
- Backup frío de entrada: digest combinado
  `a060ae47146db912ed8beacdc3ed4dc9d929f411a197345d906941c14ff2721c`.
  Backup candidato: ID `29b35dc301a04c21a188e58ed094e3eb`, contrato
  `2026-09-06.1`, tres archivos/1.037 bytes, digest de entradas
  `19bd1249e515aba5d62ec3fd20047febeaaeb2febb4e48469aed4649e381ef4a`.
- Restore correcto y rollback legible sin transformación destructiva. La suite
  backend completa pasó tras corregir PMR-007: 2.153 pruebas recolectadas.

## Pasada 3 — instalación, operación y adopción

- Compose base/privado y `make check-release` pasaron. Se construyeron desde
  cero y sin caché backend, frontend y ambos runners.
- Stack aislado con TLS: `/health` y `/ready` 200; CSP, HSTS, COOP/CORP,
  Permissions-Policy, nosniff y DENY presentes. Reiniciar backend conservó
  readiness y almacenamiento.
- Al detener `audit-tools`, health siguió 200 y readiness pasó a 503 con
  `analysis_runners=unavailable`; al recuperarlo volvió a 200. Compose falla
  cerrado si faltan hash administrativo, host o email TLS.
- CLI en `python:3.12.11-bookworm`, Git 2.39.5, sin red y tmpfs ejecutable:
  66/66 en 8,20 s. Intentos con imagen sin Git y `/tmp` noexec fallaron por esos
  recursos exactos y no se contabilizaron como validación.

## Pasada 4 — frontend y experiencia de usuario

- Navegador real local: onboarding con ZIP sintético, proyecto, análisis
  terminal con 16 hallazgos, redacción visible, `.env` detectado pero no leído y
  egress público apagado. Consola sin errores y deep link preservado.
- Sin overflow global a 320/390/768/1440 px; tablas con scroll interno. Estados
  carga, vacío, error, degradado, cancelado, sesión expirada, permiso,
  importación inválida y borrado se contrastaron con navegador y pruebas
  deterministas con axe/teclado.
- Frontend: 62 archivos/430 pruebas offline en 31,59 s; TypeScript/build verdes,
  bundle 305,7/322 KiB. PMR-004 registra los objetivos táctiles insuficientes.
  El navegador disponible no expone zoom/CDP: 200 % no pudo medirse
  directamente; un viewport equivalente 720×450 no presentó overflow. Esta
  limitación no se presenta como prueba superada.

## Pasada 5 — release y cadena de suministro

- `pip-audit` sobre cinco locks (backend runtime/dev, tools, Active y build) no
  encontró vulnerabilidades. `npm audit --package-lock-only --audit-level=high`
  con Node 22.20.0 auditó 165 paquetes: cero vulnerabilidades.
- Avisos de terceros cubren 29 distribuciones Python y seis npm runtime; el gate
  pasó. Actions usa SHAs completos, permisos de lectura, no
  `pull_request_target` y no publica artefactos no revisados.
- Dos builds independientes con `SOURCE_DATE_EPOCH=315532800` fueron idénticos.
  SHA-256: wheel `21a04b29cbba63c5838d7f541cca6a32fb015f2360343d5e536cde1f7f154ea5`;
  sdist `4722b742c992c449d3c4cd6df2025f6a8e98210f3a5d965bb0fa8c0bb9c30521`;
  SBOM `666a5b9050ffe554029f000e04d22c8235a09afcf4ca125e1f2f6e7380544580`;
  `SHA256SUMS` `0424a123b6cea999c6199fd190f92df4f7de777c9be45906217de904f16ebde9`.
  Wheel y sdist se instalaron offline e informaron `0.3.0-beta.1`.

## Validación de correcciones

- Dirigidas: Active 31/31; remediación 10/10; PMR-007 diez repeticiones verdes.
- Backend en venv limpio, contenedor read-only, sin red y tmpfs: 2.153/2.153.
  Una imagen local obsoleta falló por ausencia de PyJWT/cvss; un entorno que no
  propagó `PYTHONPATH` falló en subprocess. No se atribuyeron al sandbox ni se
  contaron como suite.
- Frontend 430/430; CLI 66/66; auditorías Python/npm cero vulnerabilidades;
  build, Compose, release, backlogs, diff-check y canario Gitleaks verdes.
- Limpieza verificada: cero temporales, proyectos Compose, contenedores,
  volúmenes, redes e imágenes con prefijo `inspectra-premerge-`; no se tocaron
  servicios ajenos.
- [GitHub Actions 34609407516](https://github.com/dreykdrk7/inspectra/actions/runs/34609407516)
  terminó correctamente sobre `5439dddcf9b451c38b75c7c500629dbca6634feb`:
  Secret scan, Python tests/audit, frontend tests/build/audit y Compose, 4/4
  verdes. El aviso de runtime Node 20 de las actions fijadas ya está registrado
  como deuda y no altera el resultado.

## Veredicto provisional y límites

**GO para marcar el PR como listo, sujeto a autorización explícita posterior.**
No quedan P0/P1 abiertos; los P2 tienen mitigación y seguimiento; actualización,
restore, instalación, flujos, artefactos, Gitleaks y auditorías son verificables;
los cuatro jobs remotos están verdes sobre el último SHA funcional. El commit
que publique este resultado es exclusivamente documental y deberá pasar el mismo
workflow; la URL estable anterior conserva la relación no circular con el SHA
funcional.

Incluso con GO: solo Linux single-host/single-worker está acreditado; Active
sigue experimental y apagado; OSV/CISA KEV conservan su alcance real y GHSA/NVD
no se presentan como integraciones reales. No se autoriza merge, ready,
aprobación, tag, publicación, release, despliegue, proyecto real ni tráfico
Active externo.
