# Placeholder Documentation Fixture

Expected class: documentation_example_marker.

Example configuration value:

```text
api_key = [REDACTED_DOCS_EXAMPLE_API_KEY]
```

The example is intentionally non-operational and should never be copied into
real configuration.
