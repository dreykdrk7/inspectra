server {
    listen 80;
    server_name example.test;
    proxy_set_header Authorization "Bearer token_should_never_render";
    location / {
        proxy_pass http://registry-user:registry-pass@example.com;
    }
}
